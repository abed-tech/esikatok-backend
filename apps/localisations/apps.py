from django.apps import AppConfig

class LocalisationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.localisations'
    verbose_name = 'Localisations géographiques'
